<!DOCTYPE html>
<html lang="pt-BR">
    <head>
        {{ include('componnents/head/registers.html.twig') }}
        <title> {{ title }} - {{SITE_NAME }}</title>
        {% block head %}{% endblock %}
    </head>
    <body class="bg-light">
        {{ include('componnents/header/registers.html.twig') }}
        <div class="container-fluid">
            <div class="row">
                {{ include('componnents/navbar/registers.html.twig') }} 
                <main role="main" class="bg-white col-md-9 ms-sm-auto col-lg-10 px-md-4">
                    {% block main %}{% endblock %}
                </main>
            </div>
        </div>
        {{ include('componnents/footer/registers.html.twig') }}

        {% block script %}{% endblock %}
        
        <script src="https://unpkg.com/feather-icons/dist/feather.min.js"></script>
        <script>
            feather.replace()
        </script>
  </body>
</html>